import { Meta, Preview, Props, Source, Story } from '@storybook/addon-docs/blocks';
import { useState } from 'react';

import Checkbox from './Checkbox';

<Meta title="Checkbox" />

# Checkbox

<Preview>
	<Story name="Default">
		{() => {
			const [checked, setChecked] = useState(false);
			return (
				<Checkbox
					checked={checked}
					label="Check me"
					name="check"
					onChange={() => {
						setChecked(!checked);
					}}
				/>
			);
		}}
	</Story>
</Preview>
